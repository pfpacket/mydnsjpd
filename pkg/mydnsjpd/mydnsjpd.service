[Unit]
Description=MyDNS.jp DDNS record updater
Requires=network.target
After=network.target

[Service]
Type=forking
ExecStart=/usr/bin/mydns_update -df /etc/mydns.conf

[Install]
WantedBy=multi-user.target
